name: spark_sdk
description: Comprehensive Dart/Flutter SDK for the Spark platform (identity, content, chat, recommendations, notifications, wallet, payment, subscription, streaming, gift, search, analytics, creator, and admin endpoints).
version: 0.2.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.1.0

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
