[project]
name = "thumbnail-ai"
version = "0.1.0"
description = "Thumbnail generation microservice for Spark platform"
requires-python = ">=3.11"
dependencies = []

[tool.ruff]
target-version = "py311"
line-length = 120

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]
