apiVersion: apps/v1
kind: Deployment
metadata:
  name: advertising-service
  namespace: spark
  labels:
    app: advertising-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: advertising-service
  template:
    metadata:
      labels:
        app: advertising-service
    spec:
      containers:
        - name: advertising-service
          image: ghcr.io/elevatecompact/advertising-service:latest
          ports:
            - containerPort: 4001
          env:
            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@$(POSTGRES_HOST):$(POSTGRES_PORT)/spark_advertising?sslmode=disable"
            - name: REDIS_URL
              value: "redis://$(REDIS_HOST):$(REDIS_PORT)/0"
            - name: KAFKA_BROKERS
              valueFrom:
                configMapKeyRef:
                  name: spark-config
                  key: KAFKA_BROKERS
            - name: LOG_LEVEL
              valueFrom:
                configMapKeyRef:
                  name: spark-config
                  key: LOG_LEVEL
          envFrom:
            - secretRef:
                name: spark-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 4001
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /ready
              port: 4001
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "150m"
              memory: "192Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: advertising-service
  namespace: spark
spec:
  ports:
    - name: http
      port: 4001
      targetPort: 4001
  selector:
    app: advertising-service
